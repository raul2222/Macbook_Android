<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:key="preferencias_principal" >
    <CheckBoxPreference
        android:key="notif"
        android:title="Mandar notificaciones"
        android:summary="Notificar si estamos cerca de un lugar"/>
    <EditTextPreference
        android:key="maxlugares"
        android:title="Máximo de lugares a mostrar"
        android:summary="Limita el número de lugares que se muestran en la lista"
        android:inputType="number"
        android:defaultValue="3"/>

    <ListPreference
        android:key="ordenacion"
        android:title="Criterio de ordenación"
        android:summary="Qué lugares van a aparecer antes"

        android:entries="@array/tiposOrdenacion"
        android:entryValues="@array/tiposOrdenacionValores"
        android:defaultValue="0"/>
    <PreferenceCategory android:title="Categoría nueva">
        <CheckBoxPreference
            android:key="notif_2"
            android:title="Pruebas"
            android:summary="Notificar si estamos cerca de una prueba"/>
    </PreferenceCategory>

</PreferenceScreen>
